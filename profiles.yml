my_first_dbt_project: # this needs to match the profile in your dbt_project.yml file
    target: dev
    outputs:
        dev:
            type: bigquery
            method: service-account
            keyfile: /Users/pierre/Downloads/bubbly-granite-456918-p0-f407c49d0214.json # replace this with the full path to your keyfile
            project: bubbly-granite-456918-p0 # Replace this with your project id
            dataset: FIL_ROUGE # Replace this with dbt_your_name, e.g. dbt_bilbo
            threads: 1
            timeout_seconds: 300
            location: US
            priority: interactive
